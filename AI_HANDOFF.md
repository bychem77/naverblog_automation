# 다른 AI에 전달하는 방법

## 가장 간단한 방법

새 AI 프로젝트에 이 저장소를 연결하고 다음과 같이 요청한다.

> `AGENTS.md`와 그 안에서 지정한 문서를 모두 읽고 바이켐 네이버 블로그 편집자로 작업해 줘. 이번 요청은 `templates/작업요청서.md`의 내용과 같아. 담당자 승인 전에는 네이버에 비공개로만 저장해.

## 저장소를 직접 연결할 수 없는 AI

다음 파일을 순서대로 첨부한다.

1. `prompts/BYCHEM_NAVERBLOG_MASTER_PROMPT.md`
2. `docs/발행운영규칙.md`
3. `prompts/이미지생성_캐릭터활용_프롬프트.md`
4. `assets/characters/`의 주제에 맞는 캐릭터 원본
5. `templates/작업요청서.md`를 채운 파일

결과 품질을 비교하려면 `examples/`에서 같은 카테고리의 최근 원고와 대표사진을 한 건 추가로 제공한다.

## 재현성의 한계

이미지 생성 모델, 웹 검색 시점, 네이버 에디터 UI와 사용 가능한 브라우저 자동화 도구가 다르면 픽셀 단위로 동일한 결과가 나오지는 않는다. 이 저장소는 문체, 정보 구조, 검수 기준, 이미지 역할, 카테고리·태그와 게시 절차를 동일하게 만드는 것을 목표로 한다.

## 예약 자동화 인수인계

현재 운영 일정은 매주 화요일·목요일 오전 9시(Asia/Seoul)이며, 회차마다 블로그 1건을 비공개 게시하고 카드뉴스 PNG를 제작한다. 정확한 실행 절차는 `docs/주간자동화운영.md`를 따른다.

예약 작업의 활성 상태와 로그인 세션은 GitHub에 저장되지 않는다. 다른 Codex 환경이나 다른 담당자 계정으로 이전할 때는 저장소를 받은 뒤 동일한 일정의 자동화를 새로 등록하고, 네이버 로그인을 직접 완료해야 한다. 아이디·비밀번호·쿠키는 저장소에 추가하지 않는다.

같은 Mac에서는 Codex 인앱 브라우저가 관리하는 로컬 네이버 세션을 재사용한다. 별도 자동화 도구가 세션 파일이나 브라우저 프로필을 요구하면 `.gitignore`에 등록된 로컬 전용 폴더에만 저장한다. 세션은 다른 컴퓨터로 Git 전송하지 않으며, 만료되거나 네이버가 재인증을 요구하면 담당자가 다시 로그인한다.
