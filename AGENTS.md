# AI 작업 지침

이 저장소에서 바이켐 네이버 블로그 관련 작업을 수행하는 모든 AI 에이전트는 작업 전에 다음 문서를 순서대로 읽고 적용한다.

1. `prompts/BYCHEM_NAVERBLOG_MASTER_PROMPT.md`
2. `docs/발행운영규칙.md`
3. 이미지 작업이면 `prompts/이미지생성_캐릭터활용_프롬프트.md`
4. 네이버 입력·발행이면 `docs/WORKFLOW.md`

예약 작업을 새 컴퓨터에 복원하거나 수정할 때는 다음 파일을 원본으로 사용한다.

1. `automations/bychem_blog_schedule.yaml`
2. `automations/BYCHEM_BLOG_AUTOMATION_PROMPT.md`
3. `automations/RESTORE_PROMPT.md`

동일한 이름의 예약 작업이 이미 있으면 중복 생성하지 말고 최신 저장소 내용으로 업데이트한다. 컴퓨터별 검토자 주소는 `automations/local.settings.yaml`에서 읽고, 해당 파일이나 로그인 세션을 GitHub에 커밋하지 않는다.

최상위 원칙:

- 승인 전에는 비공개로만 저장한다.
- 로그인 정보, 쿠키, 토큰과 브라우저 프로필을 저장소에 기록하지 않는다.
- 미확인 회사 정보는 만들지 않고 `[사내 확인 필요]`로 표시한다.
- 대표사진에는 캡션을 넣지 않는다.
- 본문 이미지 캡션은 캐릭터 행동이 아니라 기술 정보를 설명한다.
- 게시물 안팎으로 이미지 파일과 구도를 중복 사용하지 않는다.
- 모든 제목, 본문, 이미지와 스티커는 가운데 정렬한다.
- 사용자가 스티커 제외를 명시하지 않은 새 게시물에는 승인된 네이버 스티커를 최소 1개 실제로 삽입한다. 기본은 도입부의 `안녕하세요`이며, 검토만 하고 생략하지 않는다.

카드뉴스 중복 생성 방지:

- 카드뉴스 작업 전 `cardnews/data/`에서 같은 날짜·주제의 원고가 이미 등록됐는지 확인한다.
- 같은 카드뉴스 원고가 있고 블로그 원문과 카드뉴스 원고가 모두 변경되지 않았다면 새로 만들거나 다시 렌더링하지 않는다.
- 원고가 수정됐거나 템플릿·렌더러·승인 이미지가 변경된 경우, 또는 사용자가 재생성을 명시한 경우에만 다시 생성한다.

카드뉴스 표지 중복 방지:

- 카드뉴스 첫 장의 배경은 제목·문구·로고가 없는 전용 `cover_background.*`만 사용한다.
- 제목이 합성된 블로그 대표사진이나 완성 카드 `01_cover.png`를 카드뉴스 배경으로 복사하지 않는다.
- 표지 제목은 카드뉴스 템플릿에서 정확히 한 번만 합성한다.

검수 이메일 형식:

- 검수 메일은 `templates/review_email.html`을 사용해 HTML로 전송한다.
- 서식 없는 일반 텍스트 메일로 대체하지 않는다.
- 표·배경색·버튼·한줄정리 상자가 실제 메일 작성 화면에 렌더링됐는지 확인한 뒤 발송한다.
